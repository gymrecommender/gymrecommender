import React from 'react';

const NavBar = () => {
  return (
    <nav className="nav-links">
        <a href="#">Home</a>
        <a href="#">Account</a>
        <a href="#">History</a>
    </nav>
  );
};

export default NavBar;
